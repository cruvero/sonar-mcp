# sonar-mcp
Mirror the cruvero-mcp-k8s repository exactly in structure, tech stack (Go, mcp-go v0.43.2, OTEL, slog), gateway integration, Helm deployment, and runtime patterns. Replace Kubernetes client/logic with SonarQube Community Edition HTTP API client (single global token, future-proof multi-instance via instanceId). Implement MCP tools for querying iss…
